import { VisierModelQuery } from "./index";

describe("visier-model-query-typescript-sdk", () => {
    it("initialize client", async () => {
        const visiermodelquery = new VisierModelQuery({
        });
    });
});
